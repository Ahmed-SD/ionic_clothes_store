import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.colthes.app',
  appName: 'clothes store',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
